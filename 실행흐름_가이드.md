# 멀티 프로젝트 할 일 관리자 - 실행 흐름 가이드

## 📂 프로젝트 폴더 구조

```
multi-project-todo-manager/
│
├── 📄 index.html              ← ① 진입점 (HTML)
├── 📄 package.json            ← 프로젝트 설정 & 의존성
├── 📄 vite.config.js          ← Vite 빌드 도구 설정
├── 📄 tailwind.config.js      ← Tailwind CSS 설정
├── 📄 postcss.config.js       ← CSS 후처리 설정
├── 📄 jest.config.js          ← 테스트 설정
├── 📄 .babelrc                ← JavaScript 변환 설정
├── 📄 .gitignore              ← Git 제외 파일
├── 📄 README.md               ← 프로젝트 문서
│
├── 📁 src/                    ← 소스 코드 폴더
│   ├── 📄 main.jsx            ← ② React 시작점
│   ├── 📄 App.jsx             ← ③ 메인 앱 컴포넌트 (UI)
│   ├── 📄 utils.js            ← ④ 유틸리티 함수들
│   ├── 📄 utils.test.js       ← 테스트 코드 (61개)
│   └── 📄 index.css           ← Tailwind CSS
│
├── 📁 node_modules/           ← 설치된 패키지들 (npm install)
├── 📁 dist/                   ← 빌드 결과물 (npm run build)
└── 📁 coverage/               ← 테스트 커버리지 리포트
```

---

## 🔄 전체 실행 흐름

### 1단계: 명령어 실행

```bash
npm run dev
```

- 터미널에서 위 명령어 입력
- `package.json`의 `"scripts"` 섹션에 정의된 명령어 실행

### 2단계: package.json 확인

```json
{
  "scripts": {
    "dev": "vite"      ← npm run dev를 실행하면 "vite" 명령 실행
  }
}
```

### 3단계: Vite 개발 서버 시작

```bash
VITE v5.4.21  ready in 333 ms

➜  Local:   http://localhost:3000/
➜  Network: http://192.168.x.x:3000/
```

- Vite가 개발 서버를 3000번 포트에서 실행
- 파일 변경 감지 및 Hot Module Replacement (HMR) 활성화

### 4단계: 브라우저 접속

사용자가 `http://localhost:3000/`로 접속하면...

### 5단계: index.html 전송

```html
<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>멀티 프로젝트 할 일 관리</title>
  </head>
  <body>
    <div id="root"></div>                      ← React 렌더링 위치
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

**핵심 포인트:**
- `<div id="root"></div>` : React 앱이 렌더링될 컨테이너
- `<script src="/src/main.jsx">` : React 시작점 로드

### 6단계: main.jsx 실행

```javascript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';           // ← App 컴포넌트 가져오기
import './index.css';                  // ← Tailwind CSS 로드

ReactDOM.createRoot(
  document.getElementById('root')      // ← HTML의 <div id="root"> 찾기
).render(
  <React.StrictMode>
    <App />                            // ← App 컴포넌트 렌더링
  </React.StrictMode>
);
```

**역할:**
- React를 초기화
- `id="root"`인 DOM 요소를 찾음
- `App` 컴포넌트를 해당 위치에 렌더링

### 7단계: App.jsx 실행

```javascript
import React, { useState, useEffect } from 'react';
import { Check, Plus, Trash2, ... } from 'lucide-react';
import {
  loadProjects,        // ← utils.js에서 가져온 함수들
  loadSettings,
  isOverdue,
  formatDate,
  sortProjects,
  sortTasks
} from './utils.js';

export default function ProjectTaskManager() {
  // React State 초기화
  const [projects, setProjects] = useState(loadProjects);  // ← 로컬스토리지에서 프로젝트 로드
  const [settings, setSettings] = useState(loadSettings);  // ← 설정 로드

  // 프로젝트 변경 시 자동 저장
  useEffect(() => {
    localStorage.setItem('projectTaskManager', JSON.stringify(projects));
  }, [projects]);

  // UI 렌더링
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100">
      {/* 헤더: 오늘의 할 일 */}
      {/* 프로젝트 목록 */}
      {/* 할 일 목록 */}
      {/* 버튼들 */}
    </div>
  );
}
```

**역할:**
- React Hooks를 사용한 상태 관리
- 로컬스토리지에서 데이터 로드
- 사용자 인터페이스 렌더링
- 데이터 변경 시 자동 저장

### 8단계: utils.js 활용

```javascript
// 로컬스토리지에서 프로젝트 데이터 로드
export const loadProjects = () => {
  try {
    const saved = localStorage.getItem('projectTaskManager');
    if (saved) {
      return JSON.parse(saved);
    }
  } catch (error) {
    console.error('데이터 로드 실패:', error);
  }
  // 기본 데이터 반환
  return [
    {
      id: 1,
      name: '프로젝트 A',
      color: 'bg-blue-500',
      tasks: [...]
    }
  ];
};

// 마감일 체크
export const isOverdue = (dueDate) => {
  if (!dueDate) return false;
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const due = new Date(dueDate);
  due.setHours(0, 0, 0, 0);
  return due < today;
};

// 프로젝트 정렬
export const sortProjects = (projects) => {
  return [...projects].sort((a, b) => {
    // 완료되지 않은 프로젝트 우선
    if (a.completed && !b.completed) return 1;
    if (!a.completed && b.completed) return -1;
    // 마감일 빠른 순
    if (!a.endDate) return 1;
    if (!b.endDate) return -1;
    return new Date(a.endDate) - new Date(b.endDate);
  });
};
```

**역할:**
- 데이터 로드/저장 로직
- 날짜 계산 및 비교
- 정렬 알고리즘
- 진행률 계산

### 9단계: 화면 렌더링 완료

```
┌────────────────────────────────────┐
│  오늘의 할 일          ⚙️ 설정    │
│  2026년 1월 12일 일요일             │
│  진행률: 50% (5/10)                │
│  [+ 새 프로젝트 추가]              │
│  ┌─────────┐  ┌─────────┐         │
│  │프로젝트A│  │프로젝트B│         │
│  │파랑색   │  │보라색   │         │
│  │☐ 할일1  │  │☐ 할일3  │         │
│  │☐ 할일2  │  │☐ 할일4  │         │
│  └─────────┘  └─────────┘         │
└────────────────────────────────────┘
```

---

## 📊 데이터 흐름

### 사용자 입력 → 데이터 저장

```
사용자 입력
(프로젝트 추가, 할 일 체크, 마감일 설정 등)
    ↓
App.jsx에서 이벤트 처리
    ↓
React State 업데이트
(setProjects, setSettings 등)
    ↓
useEffect 훅이 변경 감지
    ↓
localStorage에 자동 저장
    ↓
React가 화면 다시 렌더링
(변경사항 즉시 반영)
```

### 페이지 로드 → 데이터 복원

```
브라우저에서 페이지 접속
    ↓
main.jsx 실행
    ↓
App.jsx 컴포넌트 마운트
    ↓
useState(loadProjects) 실행
    ↓
utils.js의 loadProjects() 호출
    ↓
localStorage에서 데이터 읽기
    ↓
저장된 데이터가 있으면 복원
없으면 기본 데이터 사용
    ↓
화면에 렌더링
```

---

## 🔧 각 파일의 역할

### 설정 파일들

| 파일 | 역할 | 설명 |
|------|------|------|
| `package.json` | 프로젝트 메타데이터 | 의존성, 스크립트, 프로젝트 정보 |
| `vite.config.js` | Vite 설정 | 빌드 도구 설정, 플러그인 등록 |
| `tailwind.config.js` | Tailwind CSS 설정 | 색상, 폰트, 반응형 설정 |
| `postcss.config.js` | PostCSS 설정 | CSS 후처리 (autoprefixer 등) |
| `jest.config.js` | Jest 테스트 설정 | 테스트 환경, 커버리지 설정 |
| `.babelrc` | Babel 설정 | JSX, ES6+ 변환 설정 |
| `.gitignore` | Git 제외 파일 | node_modules, dist 등 |

### 소스 코드 파일들

| 파일 | 라인 수 | 역할 | 설명 |
|------|---------|------|------|
| `index.html` | 14줄 | HTML 뼈대 | React 앱의 컨테이너 |
| `src/main.jsx` | 16줄 | React 진입점 | React 초기화 및 App 렌더링 |
| `src/App.jsx` | 587줄 | 메인 UI | 전체 앱 인터페이스 |
| `src/utils.js` | 186줄 | 유틸리티 | 재사용 가능한 함수들 |
| `src/utils.test.js` | 644줄 | 테스트 코드 | 61개 테스트 케이스 |
| `src/index.css` | 약간 | CSS | Tailwind CSS import |

---

## 🚀 실행 시나리오

### 시나리오 1: 최초 실행

```
1. npm install         ← 의존성 설치 (node_modules 생성)
2. npm run dev         ← 개발 서버 시작
3. 브라우저 접속        ← http://localhost:3000/
4. index.html 로드     ← Vite가 전송
5. main.jsx 실행       ← React 초기화
6. App.jsx 렌더링      ← UI 생성
7. loadProjects()      ← localStorage 확인
8. 기본 데이터 로드     ← 저장된 데이터 없음
9. 화면에 표시         ← 기본 프로젝트 2개 표시
```

### 시나리오 2: 데이터가 있는 경우

```
1. 브라우저 접속
2. App.jsx 실행
3. loadProjects() 호출
4. localStorage에서 데이터 발견
5. JSON 파싱 후 복원
6. 이전 프로젝트/할 일 모두 표시
```

### 시나리오 3: 프로젝트 추가

```
1. 사용자가 "새 프로젝트 추가" 클릭
2. 입력 폼 표시
3. 프로젝트명 입력 후 Enter
4. addProject() 함수 실행
5. setProjects([...projects, newProject])
6. useEffect 감지
7. localStorage.setItem() 실행
8. React가 화면 재렌더링
9. 새 프로젝트 화면에 표시
```

### 시나리오 4: 할 일 완료 체크

```
1. 사용자가 체크박스 클릭
2. toggleTaskCompletion() 실행
3. 해당 task의 completed 값 토글
4. setProjects(updatedProjects)
5. useEffect → localStorage 저장
6. 화면 재렌더링
7. 체크박스 체크 표시
8. 진행률 자동 업데이트
```

---

## 🔍 주요 React Hooks

### useState

```javascript
const [projects, setProjects] = useState(loadProjects);
```

- 프로젝트 데이터를 상태로 관리
- `loadProjects()`로 초기값 설정
- `setProjects()`로 업데이트

### useEffect

```javascript
useEffect(() => {
  localStorage.setItem('projectTaskManager', JSON.stringify(projects));
}, [projects]);
```

- `projects`가 변경될 때마다 실행
- localStorage에 자동 저장
- 의존성 배열: `[projects]`

---

## 🎨 스타일링 흐름

### Tailwind CSS 처리

```
1. src/index.css에서 Tailwind import
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

2. main.jsx에서 index.css import
   import './index.css';

3. Vite가 PostCSS로 처리
   tailwind.config.js 참조

4. 최종 CSS 생성 및 주입
   <style> 태그로 HTML에 주입

5. 클래스 적용
   <div className="bg-blue-500 p-4">
```

---

## 📦 빌드 프로세스

### npm run build

```
1. Vite 빌드 시작
2. 모든 JSX를 JavaScript로 변환
3. 모든 import를 번들링
4. Tailwind CSS를 최종 CSS로 컴파일
5. 코드 최적화 (minify, tree-shaking)
6. dist/ 폴더에 결과물 생성
   - index.html
   - assets/index-[hash].js   (156KB)
   - assets/index-[hash].css  (15KB)
7. 배포 준비 완료
```

---

## 🔄 Hot Module Replacement (HMR)

Vite의 HMR이 작동하는 방식:

```
1. 파일 변경 감지 (예: App.jsx 수정)
2. 변경된 모듈만 다시 컴파일
3. WebSocket으로 브라우저에 알림
4. 브라우저가 해당 모듈만 교체
5. 상태 유지하며 화면 업데이트
6. 페이지 새로고침 없이 변경사항 반영
```

**속도가 빠른 이유:**
- 전체 앱을 다시 빌드하지 않음
- 변경된 파일만 처리
- ES Modules를 네이티브로 사용

---

## 💡 핵심 포인트 정리

### 1. 진입점 흐름
```
index.html → main.jsx → App.jsx → utils.js
```

### 2. 데이터 흐름
```
localStorage → loadProjects() → useState → 화면 렌더링
사용자 입력 → setState → useEffect → localStorage 저장
```

### 3. 렌더링 흐름
```
ReactDOM.createRoot() → App 컴포넌트 → JSX 변환 → DOM 업데이트
```

### 4. 스타일 흐름
```
Tailwind classes → PostCSS 처리 → 최종 CSS → DOM 적용
```

### 5. 상태 관리
```
React State (useState) → 로컬 상태 관리
useEffect → 부수 효과 처리 (저장, API 호출 등)
```

---

## 📚 추가 학습 자료

### React 공식 문서
- https://react.dev/

### Vite 공식 문서
- https://vitejs.dev/

### Tailwind CSS 공식 문서
- https://tailwindcss.com/

---

**작성일**: 2026년 1월 12일
**작성자**: Claude
**프로젝트**: 멀티 프로젝트 할 일 관리자
**라이선스**: MIT
